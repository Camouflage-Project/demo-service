# Camouflage Demo Service

Service for load testing and demo

## Usage
- Configure in `resources/application.conf`

- execute `./gradlew run`

- navigate to `http://localhost:8085/direct` or `http//localhost:8085/proxied` to fire requests
